# these are the functions for each module. this is used for intellisense
# this file uses a simple parser so stay within the basic format show below..
# tabs and spaces are ok, comments must start with #, 
# braces should be on their own lines

nt
{
    RegRead RegDel RegWrite MsgBox ShutDown ListProcesses StartService 
    StopService PauseService ContinueService HardLink
}

mysql
{
    RealConnect Connect Close Query FetchArray FetchHash AffectedRows ChangeUser 
    CharacterSetName DataSeek ErrorMessage GetClientInfo GetHostInfo GetProtoInfo 
    GetServerInfo Info InsertId Kill Ping EscapeString SelectDatabase Shutdown 
    Stat ThreadId 
}